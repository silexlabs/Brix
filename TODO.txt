IDEAS

- 2 levels of asset embedding (local & full)

- links parsed to embed other pages HTML ?

- use in Macro Context : static function warning( msg : String, pos : Position ) : Void
Available in neko
Display a compilation warning at the given position in code


TASKS
-----------------------------------------------------
BUGS
-----------------------------------------------------
- groups => takes into account all inner inner childs
-----------------------------------------------------
- getElementByClassName => case insensitive !!!! :S
-----------------------------------------------------

Thomas : reflexions SLPlayer

- Dans le slplayer, passer à tous les display une référence au noeud racine du code embed du player 		<= obsolète ?

- attributs des noeuds doivent êtres "passés" sur les composants											<= obsolète ?
		<div class="moncomp" data-mavar="test" />
		  => moncomp = new MonComp() et moncomp.mavar = "test"
		remarque de JB

- le .js généré est référencé	???

- propriété margin et short hand	???

cf plus bas, ce qui manque au SLPlayer pour ebuzzing

compilation et verif HTML		 - 5j
modifs archi					 - 5j (fonction des choix archi à faire dès la 1ere semaine)
optim et refactoring proto		 - 5j
debugage						 - 5j


TODOS
  

@MAIN ISSUE
- EMBED DES ASSETS

@MAIN ISSUE
- EMBED HTML IN JS AND START POINT 

@MAIN ISSUE
- PRODUCE DOCUMENTATION

- a solution for the templates, not to appear at application startup ?

- an option that makes the main class changing its name and prefix all the app packages 

- copy generated HTML ?

- check what other meta data we could use/extract 

- make a set of components :
  * form set: inputs, buttons, checkboxes...
  * skinning system & examples
  

REMARKS
- there is a source HTML and a generated HTML
- classes can extend DisplayObject so that they are linked to an html node
- mixins: data consumer,

QUESTIONS
- dataProvider initialized with the content of the node "body/dataProvider"?
- comment aller plus loin dans la compilation du html / erreurs de compilation
- probleme: 2 data consumer sur le meme noeud
- composition de composant: class du sous composant (control bar) sur le meme noeud que le composant (galerie)
- The connectors could have just one instance for the all document ( or could be static classes) ?

	
	
	
DONE
- make the <scripts> not trying to download anything if the corresponding component is in SLPlayer (OK)
- CLEAN REPOSITORIES !!!
- manage when no classname in the component (!!!)
- get rid of the classnames static var ?
- check we don't initialize a component twice  (!!!)
- manage the doctypes when parsing !!!
@MAIN ISSUES
- DON'T PUT THE INDEX.HTML FILE IN SLPLAYER.HX (REFERENCE IT AT LEAST AS A COMPILER ARGUMENT (!!!)

ABORTED
- manage both syntaxes for components (with body and template and without)   [WIP]   (!!!) *** create Templatable contract ?
  * templateRenderer with reverse template syntax	[WIP]	(!!!)
- skinning: class styles on the parts of the components, used also to get references in the component class, and also you can redefine the containers  (!!!)
- parse the metadata and extract the viewport 

------------------------
Discussion lundi soir :

- pour les surdefinition de skins :
	
	<div class="playerimg-first-button

- pour les templates et bodys :	

	<imgpl
		<body data-body=""
		<template data-tmpl=""

- phase init slplayer ?

- embed html dans .js

- make possible to put several cmps in data-slp-use


------------------------
Specs discussions :

- créer class="buzzplayer" pour definir des super composant => interdire les groupes imbriqués ? Tous les sous elts d'un groupes envoient et ecoutent les event sur le node du groupe

- parser HTML a la recherche de : 
	
	* conflits de classname
	* html invalide ?

- embedder HTML dans .js =>  juste le body ?!

